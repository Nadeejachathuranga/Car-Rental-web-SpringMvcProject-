var uid;
function passwordValidation(userName,password) {
     uid=userName;
   // alert("invoked");
    if (userName=="c005" && password=="nad" ){
       return true;
    }else {
       return false;
    }
}